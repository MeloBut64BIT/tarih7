name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Manuel olarak çalıştırmak için

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build web app
      run: npm run build
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Add Android platform (if not exists)
      run: |
        if [ ! -d "android" ]; then
          npx cap add android
        fi
      
    - name: Sync Capacitor
      run: npx cap sync
      
    - name: Grant execute permission for gradlew
      run: chmod +x android/gradlew
      working-directory: .
      
    - name: Build APK
      working-directory: ./android
      run: ./gradlew assembleDebug
      
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug.apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
        
    - name: Create Release (only on push to main/master)
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: softprops/action-gh-release@v1
      with:
        files: android/app/build/outputs/apk/debug/app-debug.apk
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        body: |
          APK otomatik olarak build edildi.
          
          📱 **APK'yı indirmek için:**
          1. Bu sayfanın altındaki "Assets" bölümünden `app-debug.apk` dosyasını indirin
          2. Telefonunuza kopyalayın
          3. Ayarlar → Güvenlik → Bilinmeyen Kaynaklardan Yükleme iznini verin
          4. APK dosyasına tıklayarak yükleyin
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

